# Modbus TCP 从站协议

固件在 STA 获得 IP 后，于该 STA 地址的 TCP `502` 端口启动 Modbus TCP 从站。
监听器不会绑定开放的 softAP 地址；STA 断线或 WiFi 驱动重启时，现有连接关闭，
恢复联网后自动重新监听。一次只服务一个主站连接。

Modbus TCP 是主站轮询协议，不主动向服务器“提交”数据。主站通过功能码 `01/02/03/04`
读取运行数据，通过 `05/06/0F/10` 下发控制。MBAP Unit Identifier 不限定，响应会
原样回显请求值。寄存器地址均为协议 PDU 中的零基地址；某些软件界面显示为
`00001`、`30001`、`40001` 等一基编号，使用时需按软件规则换算。

Modbus TCP 本身没有认证和加密。能访问设备 STA 局域网的主站即可执行本文档中的
写操作，部署时应使用受控 VLAN、防火墙或其他网络隔离措施。

多字数据统一使用高字在前：地址较小的寄存器是 `uint32` 的高 16 位。

## 线圈（功能码 01/05/0F）

| 零基地址 | 内容 | 访问 |
|---:|---|---|
| `0x0000` | DO0 实际状态/手动控制 | 读写 |
| `0x0001` | DO1 实际状态/手动控制 | 读写 |

写单线圈只接受标准值 `0xFF00`（ON）和 `0x0000`（OFF）。启用的联动规则可能在
下一次计数变化时重新覆盖手动输出。

## 离散输入（功能码 02）

| 零基地址 | 内容 |
|---:|---|
| `0x0000`–`0x0001` | DO0–DO1 实际状态 |
| `0x0002`–`0x0003` | 联动规则启用状态 |
| `0x0004`–`0x0005` | 联动表达式当前计算结果 |
| `0x0006`–`0x0007` | 联动规则有效状态 |

## 输入寄存器（功能码 04，只读）

允许读取 `0x0000`–`0x0031` 的连续区间；表中未分配的保留地址返回 `0`。

| 零基地址 | 内容 |
|---:|---|
| `0x0000` | 映射版本，当前为 `0x0100` |
| `0x0001` | 计数通道数量，当前为 `4` |
| `0x0002` | 输出数量，当前为 `2` |
| `0x0010`–`0x0011` | counter0，uint32 高字/低字 |
| `0x0012`–`0x0013` | counter1，uint32 高字/低字 |
| `0x0014`–`0x0015` | counter2，uint32 高字/低字 |
| `0x0016`–`0x0017` | counter3，uint32 高字/低字 |
| `0x0020` | 输出位图，bit0=DO0、bit1=DO1 |
| `0x0021` | 联动启用位图，bit0=OUT0、bit1=OUT1 |
| `0x0022` | 联动有效位图 |
| `0x0023` | 联动表达式结果位图 |
| `0x0024` | 联动 true 电平位图 |
| `0x0030`–`0x0031` | 设备运行秒数，uint32 高字/低字 |

## 保持寄存器（功能码 03/06/10）

| 零基地址 | 内容 | 访问及约束 |
|---:|---|---|
| `0x0000` | 命令寄存器 | 读为 0；仅用 FC06 写入 |
| `0x0010`–`0x0011` | counter0 | 读；FC10 必须从高字起一次写 2 个寄存器 |
| `0x0012`–`0x0013` | counter1 | 同上 |
| `0x0014`–`0x0015` | counter2 | 同上 |
| `0x0016`–`0x0017` | counter3 | 同上 |
| `0x0020` | 输出位图 | 读；FC06 写，值只允许 `0`–`3` |

命令寄存器写入后立即执行，不保存命令值：

| 写入值 | 操作 |
|---:|---|
| `0x0001`–`0x0004` | 分别复位 counter0–counter3 |
| `0x0010` | 复位全部计数器 |
| `0x0021`–`0x0024` | 分别将 counter0–counter3 加一 |

配置、WiFi/MQTT 凭据、Topic、联动表达式和 OTA 不映射到 Modbus。它们属于字符串、
凭据或事务型操作，继续通过 softAP TCP JSON 管理，避免产生不完整写入和越权配置。

## 异常响应

- `01`：不支持的功能码。
- `02`：地址或地址范围不在映射内。
- `03`：数量、长度、线圈值、命令值或寄存器值非法。
- `04`：设备控制层执行失败。
